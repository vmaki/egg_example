'use strict';

/**
 * @param {Egg.Application} app - egg application
 */
module.exports = app => {
  const { router, controller } = app;
  router.get('/', controller.home.index);
  router.get('/crypto', controller.home.crypto);
  router.get('/fibonacci', controller.home.fibonacci);
};
